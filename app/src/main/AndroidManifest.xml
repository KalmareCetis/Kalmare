<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kalmare"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Respira"></activity>
        <activity android:name=".Mindful"></activity>
        <activity android:name=".CincoUno"></activity>

        <activity android:name=".InfoAns"></activity>
        <activity android:name=".Psicologia"></activity>
        <activity android:name=".Sintomas"></activity>
        <activity android:name=".Causas"></activity>

        <activity android:name=".Milo"></activity>
        <activity android:name=".Musica"></activity>
    </application>

</manifest>